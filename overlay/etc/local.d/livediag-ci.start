#!/bin/sh
# Run the unattended suite only when the boot token marks this as CI.
# On a normal boot this file does nothing at all.
case "$(/usr/local/bin/livediag-token 2>/dev/null)" in
*livediag.ci*) /usr/local/bin/livediag-ci ;;
esac
